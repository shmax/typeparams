export interface TypeparamsConfig {
    /**
     * Directory where the generated schemas will be saved.
     * If not provided, defaults to the `.generated-schemas` folder in the current working directory.
     */
    outputDir?: string;

}